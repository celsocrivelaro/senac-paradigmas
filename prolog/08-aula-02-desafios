conectado(a, b).
conectado(b, c).
conectado(c, d).
conectado(b, e).

caminho(X, Y, [X, Y]) :- conectado(X, Y).
caminho(X, Y, [X|T]) :-
    conectado(X, Z),
    caminho(Z, Y, T),
    \+ member(X, T).

